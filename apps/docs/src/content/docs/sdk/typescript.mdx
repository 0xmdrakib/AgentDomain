---
title: TypeScript SDK
description: Use AgentDomain SDK 0.10.0 for independent identity inspection, renewal planning, resumable registration, DNS and email.
---

## Install

```bash
npm install @agentdomain/sdk@0.10.0 viem@2.56.3
```

The default API base is `https://api.agentdomain.app/api/v1` and the default network is Base
mainnet.

[SDK 0.10.0 is published on npm](https://www.npmjs.com/package/@agentdomain/sdk/v/0.10.0).
The generated machine-readable catalog records workspace versions separately;
its source manifest does not query or establish registry publication.

## Independent identity inspection

`inspectAgentIdentity` is included in the published `@agentdomain/sdk@0.10.0`
package. The older 0.9.1 release does not include it. For development from the
reviewed source checkout instead of the npm package, build from the repository root:

```bash
pnpm --filter @agentdomain/shared build
pnpm --filter @agentdomain/sdk build
```

```ts
import { inspectAgentIdentity, IdentityInspectionError } from '@agentdomain/sdk';

try {
  const result = await inspectAgentIdentity({ tokenId: '1' });
  if (result.status === 'found') {
    console.log(result.block, result.nftOwner, result.lifecycle, result.checks);
  } else {
    console.log('No identity at this block', result.block, result.tokenId);
  }
} catch (error) {
  if (error instanceof IdentityInspectionError) {
    console.log(error.code, error.message);
  } else {
    throw error;
  }
}
```

This standalone function does not construct `AgentDomain`, call the platform
API, require an API key or wallet, sign, write, or fetch metadata. Its input is
exactly one domain or token ID, with an optional expected owner:

```ts
await inspectAgentIdentity({ domain: 'example.xyz' });
await inspectAgentIdentity({
  tokenId: '1',
  expectedOwner: '0x1111111111111111111111111111111111111111',
});
```

These inputs are illustrative, not assertions that the domain is registered or
the example address owns token 1. Domains are normalized to lowercase after
trimming; they must be full ASCII domains of at most 253 characters, with labels
of at most 63 characters and no URL, path or trailing dot. Token IDs are trimmed,
positive decimal uint256 strings without leading zeros. Expected owners must be
valid Ethereum addresses; invalid mixed-case checksums are rejected.

### Results

Both result variants are JSON-safe and include `version: 1`, `chainId: 8453`,
`registryAddress`, normalized `input`, and `block` with decimal-string `number`
and Unix-seconds `timestamp`, a hex `hash`, and `tag: "safe"`.

| Field                         | Meaning                                                                                                                                                                           |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `status: "found"`             | The registry returned an identity. This alone is not a successful trust verdict.                                                                                                  |
| `identity`                    | Stored owner, domain, Basename, ENS name, metadata URI, creation/expiry times and revocation flag. Times are decimal strings.                                                     |
| `nftOwner`, `metadataUri`     | Separately read ERC-721 owner and token URI for comparison with the stored identity.                                                                                              |
| `lifecycle`                   | `revoked`, `expired` or `active`, derived using the observed block's timestamp.                                                                                                   |
| `checks`                      | Owner, domain mapping, metadata URI and lifecycle consistency; optional expected-owner match.                                                                                     |
| `consistent`                  | Only the four internal consistency checks combined. It does not include `expectedOwnerMatches`.                                                                                   |
| `checks.expectedOwnerMatches` | `null` if omitted, otherwise whether the expected address matches the NFT owner. It is not authentication.                                                                        |
| `status: "not_found"`         | No domain mapping, or the exact missing-token revert. `tokenId` is `null` for an unmapped domain, or the requested token ID for a missing token. No identity fields are invented. |

Reads use the canonical custom AgentDomain ERC-721 registry at
`0x234a2B83B32910436A35CDa797CCC57988B9Bd15`. The SDK checks the RPC's chain ID,
reads one `safe` block, and pins every contract call using the EIP-1898 selector
`{ blockHash: safeHash, requireCanonical: true }`. Domain lookup and identity
lookup are direct calls. The four consistency reads use an explicit Base
Multicall3 batch at that same canonical hash. The SDK rechecks the block's hash,
timestamp and the chain before returning. It never falls back to a numeric block
selector or `latest`. This contract is not the ERC-8004 registry.

### Errors and provider choice

`IdentityInspectionError` exposes a stable `code` and sanitized `message`:

| Code              | Meaning                                                                                                         |
| ----------------- | --------------------------------------------------------------------------------------------------------------- |
| `INVALID_INPUT`   | Invalid lookup input, or an injected client that does not explicitly set `ccipRead: false`.                     |
| `WRONG_CHAIN`     | The RPC reports a network other than Base mainnet, including a network change during the read.                  |
| `UNSAFE_SNAPSHOT` | A safe snapshot is unavailable or its block evidence changed.                                                   |
| `UNAVAILABLE`     | The read could not produce a reliable observation; includes provider failures and conflicting registry records. |

An error is not `not_found`. The default `https://mainnet.base.org` provider is
rate limited and uses an eight-second transport timeout with no retries.
Production callers should manually choose and inject a trusted Base RPC client:

```ts
import { createPublicClient, http } from 'viem';
import { base } from 'viem/chains';
import { inspectAgentIdentity } from '@agentdomain/sdk';

const publicClient = createPublicClient({
  chain: base,
  ccipRead: false,
  // Select an operator-approved Base provider here.
  transport: http('https://mainnet.base.org', { timeout: 8_000, retryCount: 0 }),
});

await inspectAgentIdentity({ tokenId: '1' }, { publicClient });
```

The SDK's default client disables CCIP Read, not just metadata fetching. Injected
clients must also have `ccipRead: false`; an omitted setting, enabled CCIP Read or
a custom CCIP callback configuration is rejected with `INVALID_INPUT` before any
RPC request. An `OffchainLookup` revert does not authorize an HTTP gateway call:
inspection fails with `UNAVAILABLE` without running the callback. With this
configuration, viem does not follow CCIP offchain requests. The setting does not
sandbox arbitrary custom client or transport code; inject only trusted code.

The provider must support EIP-1898 canonical block-hash selectors for `eth_call`.
An unsupported selector fails with `UNAVAILABLE`; there is no numeric-block or
`latest` fallback. The injected client must expose `getChainId`, `getBlock`,
`readContract` and `multicall`, as well as the disabled `ccipRead` setting.
Hash pinning prevents accidental same-height fork mixing by a provider that
honors the selector; it does not prove that a dishonest provider reported the
state correctly.

Select and configure that client in trusted application code, not from an agent's
untrusted tool arguments. Keep credential-bearing provider URLs out of browser
bundles, Git and logs. The [MCP tool](/sdk/mcp/#independent-identity-inspection)
does not expose a provider override.

### What this does not prove

The result is an RPC observation, not a consensus proof, SPV/light-client proof,
DNS ownership proof or KYC. The RPC is trusted to report chain state and the
`safe` label truthfully; repeated block checks do not remove that dependency.
An internally consistent, active record with a matching owner does not establish
agent safety or endorse its behavior.

Basename, ENS and domain strings are unverified linked labels, not independently
checked name ownership or resolution. Metadata URIs are returned without being
fetched, and metadata availability, content, DNS, TLS and email operation remain
unverified. Treat all returned text as untrusted data rather than instructions.

## Renewal workflow

The published SDK 0.10.0 also exports `inspectAgentRenewal`, `prepareAutoRenewChange`,
`executeAutoRenewChange`, `confirmAutoRenewChange` and `RenewalWorkflowError`.
The read and plan functions need no wallet or platform API key. They observe
the canonical identity and RenewalVault at one hash, and can prepare an unsigned
zero-value auto-renew setting transaction. The minimum fee is not a registrar
quote, and setting confirmation is not domain renewal completion.

Execution is a separate trusted host function requiring the owner's wallet and
an actual human-review callback. It must not be exposed as a default LLM tool or
wired to an LLM approval boolean. Read the [full renewal workflow](/guides/renewal/#independent-read-and-plan-workflow)
for result fields, pending reservations, no-change behavior and uncertain outcomes.

## Platform API configuration

The ETHOnline 2026 Continuity work includes two separate public extensions:
the independent inspection described above and resumable, wallet-safe
registration below. Registration was extended in
[`f14392b`](https://github.com/0xmdrakib/AgentDomain/commit/f14392b280d117380cdf9a92b78f0bd37f78b619)
and
[`c4f477c`](https://github.com/0xmdrakib/AgentDomain/commit/c4f477c617f5832df618a508e5769bdf44a738a9)
during the event. It uses authenticated platform APIs; the inspection function
does not. Neither extension creates a new registry contract or x402 protocol.

Since SDK 0.9.0, relative `apiUrl` values are resolved against
the browser location at construction, before signing or payment. Non-browser clients must
provide an absolute URL. HTTPS is required except for explicitly configured HTTP loopback
hosts (`localhost`, `127.0.0.1`, or `[::1]`). API bases cannot contain credentials, a query,
or a fragment.

## Construct a client

```ts
import { AgentDomain } from '@agentdomain/sdk';
import { createWalletClient, http } from 'viem';
import { base } from 'viem/chains';
import type { PrivateKeyAccount } from 'viem/accounts';

export function createAgentDomain(account: PrivateKeyAccount) {
  const walletClient = createWalletClient({ account, chain: base, transport: http() });

  return new AgentDomain({ walletClient });
}
```

Supply the account through your wallet or secret-management integration. For agent-scoped server
operations, construct `AgentDomain` with an `apiKey` instead and keep that value server-side.

## Quote and register

```ts
const quote = await agentDomain.quote({
  preferredName: 'research-agent',
  tld: 'ai',
  registerBasename: false,
  registerEns: false,
  emailUsername: 'agent',
  years: 1,
});

const identity = await agentDomain.register({
  preferredName: 'research-agent',
  tld: 'ai',
  registerBasename: false,
  registerEns: false,
  emailUsername: 'agent',
  years: 1,
});

console.log(quote.totalUsdc, identity.agentId, identity.provisioningStatus);
```

`register()` requires the payer's wallet address, either in the request or from the wallet client.
The connected signer must match that payer; use `ownerAddress` for a different designated NFT
owner. When the API returns an x402 challenge, the SDK signs and submits the exact challenged
request once on Base mainnet. It does not automatically replay a paid request after an unknown
outcome.

## Request-bound payment signing

SDK 0.9.1 fixes the duplicate payment-signature prompt for request-bound checkout. The SDK
creates one EIP-3009 payment signature using the issued request binding, preserving the complete
selected x402 requirement as `accepted`, including every `extra` field unchanged. This is one
payment signature, not a promise that wallet authentication will never request a separate signature.

Before signing, it validates the exact Base-mainnet scheme/network, Base USDC asset, nonzero
recipient, USDC EIP-712 domain, positive uint256 amount, authorization timeout, and request binding.
When `extra.quoteExpiresAt` is present, it must be an unexpired ISO timestamp and limits the
authorization expiry. These checks do not independently compare an earlier display quote or
impose an application spending budget.

The registration API's initial `402` supplies `accepts[0].extra.registrationQuote` as an opaque
string and `accepts[0].extra.quoteExpiresAt` as an ISO timestamp. Echo the accepted requirement
unchanged: do not parse or reconstruct the sealed quote, discard metadata, or reprice the request.
Once the signed request has been submitted, never automatically sign a replacement after an
ambiguous outcome, even if a later response includes another challenge or the quote expires.

### Payment rejection errors

SDK 0.9.1 exports `RegistrationPaymentRejectedError` for an explicit HTTP `4xx` response other
than `408` that satisfies `registrationPaymentRejectionSchema`, including
`paymentSubmission: { status: 'rejected', settlementAttempted: false }`. The reusable public
schema is exported by `@agentdomain/shared` 0.9.1. Both the HTTP status restriction and the valid
payload are required; an HTTP error code, conflict, or message alone is not conclusive.

| Error field           | Meaning                                                                     |
| --------------------- | --------------------------------------------------------------------------- |
| `code`                | `REGISTRATION_PAYMENT_REJECTED`.                                            |
| `serverCode`          | Server diagnostic code, separate from the SDK error code.                   |
| `message`             | Diagnostic message; do not parse it to classify payment outcomes.           |
| `handle`              | Submission handle to retain for reference and payer-authenticated recovery. |
| `settlementAttempted` | Always `false` for this error.                                              |

This error confirms rejection before settlement was attempted for this submission. It does not
assert payment success or decide an earlier attempt's outcome. HTTP `408`, `5xx`, timeouts,
network failures, lost responses, and unconfirmed conflicts (such as a `409` without the valid
rejection contract) remain `RegistrationPendingError` recovery cases. A rejection marker in a
`408` or `5xx` body does not make it a safe rejection. Neither error authorizes automatic
resending, repricing, or signing a replacement payment.

## Asynchronous registration

New in 0.9.0: submission, status, waiting, and recovery methods distinguish payment acceptance
from completed registration. SDK 0.8.x and earlier may treat HTTP `202` as a complete
`RegistrationResult`; do not infer readiness from their return types. Use SDK 0.10.0 or integrate
the [HTTP status contract](/api-reference/register/) directly.

```ts
import { RegistrationPaymentRejectedError, RegistrationPendingError } from '@agentdomain/sdk';

const controller = new AbortController();
try {
  const submission = await agentDomain.submitRegistration(
    { preferredName: 'research-agent', tld: 'com' },
    { signal: controller.signal },
  );
  const result =
    'status' in submission
      ? await agentDomain.waitForRegistration(submission, {
          timeoutMs: 600_000,
          signal: controller.signal,
        })
      : submission;
  console.log(result.agentId);
} catch (error) {
  if (error instanceof RegistrationPaymentRejectedError) {
    console.log(error.code, error.serverCode, error.message, error.handle);
    // Review this rejected submission. Do not automatically resend payment.
  } else if (error instanceof RegistrationPendingError) {
    console.log(error.reason, error.handle);
    // Read status later with the payer wallet. Do not start another paid checkout.
  } else {
    throw error;
  }
}
```

`register(args, { timeoutMs, signal })` combines submission and waiting. It sends
`Prefer: respond-async`, preserves legacy `200` results, and handles `202` by polling GETs only.
Submission and individual reads default to a 60-second deadline; the wait defaults to ten minutes
after acceptance. The wait timeout does not extend a submission deadline. Polling honors the
server's interval and transient-read `Retry-After` with a five-second minimum and stops when user
action is required.

`getRegistration(id)` returns `RegistrationProgress`; `getRegistrations({ limit, offset })` returns
the payer's paginated list, defaulting to 20 entries with a maximum of 50. These methods never
authorize with an agent API key or payment header.
Wallet clients sign `agentdomain.app api auth <timestampMs>` for `X-Agent-Signature`; the SDK caches
the signature in memory for up to four minutes and refreshes before its five-minute expiry.
Signing rejection and authorization failure stop the waiter, avoiding repeated wallet prompts.
Signature-authenticated registration requests omit cookies so another wallet's SIWE session
cannot override the signer. Status, list, and recovery reads send `expectedPayer` and verify
`X-Authenticated-Wallet` on successful responses before decoding their bodies. The query parameter
does not authenticate the request; it checks the authenticated identity against the intended payer.

For an existing browser SIWE session, construct the client with `registrationAuth: 'session'`.
Without a wallet client, also supply `registrationExpectedPayer` in the constructor or
`expectedPayer` per request. Cookies are included and reads do not prompt the wallet. Unbound
session-only reads fail locally, while a cookie belonging to another payer is rejected by the API.
Use the same host for sign-in and reads.
The payer is the authorized registration reader even when another wallet will own the NFT.

On `completed`, the waiter returns `progress.result`. It never manufactures missing token IDs,
transaction hashes, or metadata. Missing legacy results produce a pending recovery diagnostic.
`RegistrationFailedError` represents a server-confirmed `failed` or `refunded` status;
`RegistrationPendingError` represents timeout, abort, an unknown outcome, or an actionable state.
Neither is an instruction to pay again.
Minimal `202 action_required` responses also raise `RegistrationPendingError` with a reusable
handle, without a second payment attempt.

After a lost submission response, use its error handle with `getRegistration(handle.registrationId)`
when an ID is known, or `recoverRegistration(handle)` when unknown. Recovery performs only
payer-authenticated list/detail reads, matching the exact domain and inclusive submission time
window. It scans at most ten pages of 50 entries (500 total) and refuses multiple matches, no match, or an
incomplete search. Clock skew can prevent matching; no match is not proof that payment failed.
No transport error triggers an automatic payment POST retry.
SDK-generated pending handles also retain `payerAddress`; recovery cannot silently switch to a
different signing wallet. Session-only callers can pass `handle.payerAddress` as `expectedPayer`
when calling a detail read directly.
Known-ID recovery checks the same domain, inclusive time window, and authenticated payer as
unknown-ID discovery. An inconsistent handle is rejected rather than returning another purchase.
When only the registration ID is known, use an authenticated `getRegistration()` call.

HTTP `200` registration results are also schema-projected: required fields must be present,
the returned domain must match the submitted request, and unknown fields are stripped,
including unknown nested snapshot fields. Public snapshot values and items are retained without
repricing or truncation. Invalid responses retain a pending handle and never trigger another payment.

## Upgrading from 0.8.x

Existing `register(args)` calls remain valid, but HTTP `202` now starts authenticated waiting.
Use `submitRegistration()` for acceptance without waiting for asynchronous provisioning, and
handle the structured pending and failed errors above. Registration does not use an agent API key for authentication. Custom
API adapters must implement the documented status contract and expose `X-Authenticated-Wallet`.
Constructor URL validation also applies to clients used only for discovery.

Align/deduplicate viem to the reviewed 2.56.3 version across your client code and SDK dependencies.
An independently pinned 2.55.8 `PublicClient` lacks the required `watchBlockHeaders` member and
can fail TypeScript assignment. Arbitrary mixed-version viem compatibility is not guaranteed;
SDK import paths and ESM-only packaging are unchanged.

The x402 packages remain aligned at 2.25.0. The payment helper does not impose the upstream
default $1 spending cap. Unbound payments retain the existing upstream asset-selection behavior;
request-bound payments use the Base USDC validation above. Neither path independently compares
an earlier display quote or imposes an application spending budget; callers must authorize the
payment terms.

## Email and DNS

```ts
await agentDomain.sendEmail(agentId, {
  to: 'operator@example.com',
  subject: 'Status',
  text: 'Agent online.',
  idempotencyKey: crypto.randomUUID(),
});

const records = await agentDomain.listDnsRecords(agentId);
const preview = await agentDomain.previewDnsBatch(agentId, desiredRecords, 'merge');
await agentDomain.applyDnsBatch(agentId, desiredRecords, preview.baseRevision, 'merge');
```

Use [Email API](/api-reference/email/) and [DNS API](/api-reference/dns/) limits when designing
automation.

## Owner-controlled auto-renew

Direct Base writes require the public RenewalVault address and a public ERC-8021 builder code:

```ts
import type { Address } from 'viem';

const agentDomain = new AgentDomain({
  walletClient,
  publicClient,
  renewalVaultAddress: '0xb7b19826a566ebd9e8b50bee986a8da04bbb2c0a' as Address,
  builderCode: 'my_agent_app',
});

const status = await agentDomain.getRenewalStatus(agentId);
if (Number(status.shortfallUsdc) > 0) {
  await agentDomain.fundRenewalVault(agentId, status.shortfallUsdc);
}
await agentDomain.setAutoRenew(agentId, true, { waitForReceipt: true });
```

The wallet client must be the AgentID NFT owner for `setAutoRenew()`. The builder code contains 1
to 32 lowercase letters, numbers, or underscores and is public attribution, not a credential.

## Registry and plans

The SDK also includes registry search, agent lookup, API key management from owner sessions,
service-plan status and purchase, registry visibility, email usage and webhooks, DNS import/export,
and owner-authorized vault withdrawal.

Read the [API error contract](/api-reference/overview/#errors) and handle stable error codes rather
than parsing message text.
