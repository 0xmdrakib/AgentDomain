{
  "name": "@agentdomain/ssl-provisioner",
  "version": "0.1.0",
  "private": true,
  "description": "Let's Encrypt automation for AgentDomain (HTTP-01 / DNS-01 ACME)",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"no lint\""
  },
  "dependencies": {
    "@agentdomain/shared": "workspace:*",
    "acme-client": "^5.4.0",
    "drizzle-orm": "^0.38.2",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
